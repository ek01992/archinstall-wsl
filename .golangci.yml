run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - gosimple
    - ineffassign
    - typecheck
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
# Seam detection is handled by CI grep step (see .github/workflows/ci.yml)
